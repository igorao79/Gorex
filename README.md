# Gorex - Командный Task-менеджер

Командный task-менеджер с Kanban-доской, проектами и аналитикой. Упрощенный Trello для вашей команды разработчиков.

## 🚀 Возможности

- ✅ Регистрация и авторизация пользователей
- ✅ Создание и управление проектами
- ✅ Приглашение участников по email
- ✅ Создание задач с приоритетами и дедлайнами
- ✅ Kanban-доска с drag-and-drop
- ✅ Статусы задач: To Do → In Progress → Review → Done
- ✅ Приоритеты: Low / Medium / High / Urgent
- ✅ Базовая аналитика и отчеты

## 🛠 Технический стек

- **Frontend**: Next.js 14 (App Router), React 19, TypeScript
- **UI**: Tailwind CSS, Shadcn/ui
- **Backend**: Next.js API Routes
- **Database**: PostgreSQL с Prisma ORM
- **Authentication**: NextAuth.js
- **Drag & Drop**: @hello-pangea/dnd
- **Forms**: React Hook Form
- **Charts**: Chart.js

## 📋 Требования

- Node.js 18+
- PostgreSQL
- npm или yarn

## 🚀 Быстрый старт

1. **Клонируйте репозиторий и установите зависимости:**
```bash
cd gorex
npm install
```

2. **Настройте базу данных:**

**Вариант 1: PostgreSQL локально (Windows)**
```bash
# Установите PostgreSQL: https://www.postgresql.org/download/windows/
# После установки откройте pgAdmin или psql и выполните:
# CREATE DATABASE gorex;
```

**Вариант 2: Docker (рекомендуется)**
```bash
# Установите Docker: https://www.docker.com/products/docker-desktop
docker run --name gorex-postgres -e POSTGRES_PASSWORD=password -e POSTGRES_DB=gorex -p 5432:5432 -d postgres:15
```

**Вариант 3: Облачная БД (Supabase, Railway, PlanetScale)**
```bash
# Используйте бесплатные облачные сервисы для PostgreSQL
# Обновите DATABASE_URL в .env.local
```

3. **Настройте переменные окружения:**
```bash
cp env-example.txt .env.local
```

Отредактируйте `.env.local` и укажите:
- `DATABASE_URL` - URL подключения к PostgreSQL
- `NEXTAUTH_SECRET` - секретный ключ (сгенерируйте с `openssl rand -base64 32`)
- `NEXTAUTH_URL` - URL вашего приложения (для разработки `http://localhost:3000`)

4. **Запустите миграции базы данных:**
```bash
npx prisma migrate dev
```

5. **Запустите сервер разработки:**
```bash
npm run dev
```

6. **Откройте [http://localhost:3000](http://localhost:3000)**

## 📁 Структура проекта

```
gorex/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/               # API маршруты
│   │   ├── auth/              # Страницы аутентификации
│   │   ├── dashboard/         # Dashboard
│   │   └── globals.css        # Глобальные стили
│   ├── components/            # React компоненты
│   │   ├── ui/               # Shadcn/ui компоненты
│   │   ├── dashboard/        # Компоненты dashboard
│   │   ├── layout/           # Layout компоненты
│   │   └── providers/        # Context providers
│   └── lib/                  # Утилиты и конфигурация
├── prisma/
│   └── schema.prisma         # Схема базы данных
└── public/                   # Статические файлы
```

## 🗃 Схема базы данных

- **User** - пользователи
- **Project** - проекты
- **ProjectMember** - участники проектов
- **Task** - задачи
- **Account, Session, VerificationToken** - NextAuth модели

## 📈 Roadmap (MVP)

- [x] Базовая структура проекта
- [x] Аутентификация пользователей
- [x] Схема базы данных
- [ ] API для проектов
- [ ] API для задач
- [ ] Kanban-доска с drag-and-drop
- [ ] Аналитика и графики
- [ ] Приглашения участников

## 🤝 Команда

MVP разработан для команды из 5 человек за выходные.

## 📄 Лицензия

MIT
